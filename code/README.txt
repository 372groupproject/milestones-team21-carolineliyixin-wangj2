----------------------------------milestone 2 --------------------------------------
use command lua p2_x.lua run the code for control structure.
use command lua -i p2_featrue.lua and then call sum(n) to run the program 
and the program is use the repeat control structure to compute the sum of the number which user decide.


----------------------------------milestone 3 --------------------------------------
p3_boolean.lua, p3_nil.lua, p3_number.lua, p3_string.lua and p3_table.lua are use to represent the different data type in Lua. Use command lua p3_fratue.Lua can directly compile and run the program and see the output.

p3_function.lua is function data type and need use command lua -i p3_function.lua and then use print(sum_even(n))(n is the random number you choose). The result is the sum of the even number small then the number you choose.

p3_program.lua is the small program that use different data type. Use command lua -i p3_program.lua and number_table(x) (x is one element in the table).

p3_error.lua is the error handing in the Lua. Use command lua p3_error.lua and then can see the  error message.